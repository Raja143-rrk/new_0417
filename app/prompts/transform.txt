Convert the given {object_type} from {source_db} to {target_db}.

Target rules:
{target_rules}

Object-specific rules:
{object_rules}

Additional constraints:
{extra_rules}

RAG context:
{rag_context}

Use the RAG context above. Apply its examples, known issues, and procedural guidance when generating the target SQL.

Object name: {object_name}
Source SQL:
{source_sql}
