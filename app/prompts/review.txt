Review and correct the transformed SQL for a {object_type} migration from {source_db} to {target_db}.

Target rules:
{target_rules}

Object-specific rules:
{object_rules}

Additional constraints:
{extra_rules}

RAG context:
{rag_context}

Use the RAG context above. Preserve valid target syntax and apply procedure-specific guidance strictly.

Object name: {object_name}
Source SQL:
{source_sql}

Current transformed SQL:
{transformed_sql}
